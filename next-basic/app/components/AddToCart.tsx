"use client";
import React from "react";

const AddToCart = () => {
  return (
    <button
      onClick={() => {
        console.log("hello");
      }}
    >
      Add TO cart
    </button>
  );
};

export default AddToCart;
